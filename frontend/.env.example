# Copiar a .env si necesitas apuntar a URLs distintas a las de desarrollo local por defecto.
VITE_ORDERS_API_URL=http://localhost:5081
VITE_INVENTORY_API_URL=http://localhost:5080

# Frecuencia (ms) del polling de la lista de pedidos.
VITE_POLL_INTERVAL_MS=4000
